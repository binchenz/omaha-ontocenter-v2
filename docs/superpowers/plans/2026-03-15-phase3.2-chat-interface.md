# Phase 3.2: Chat Interface + ECharts Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an embedded chat interface with multi-turn dialogue, LLM function calling, and automatic ECharts visualization.

**Architecture:** ChatService orchestrates LLM function calling to MCP tools (via internal API), backend rule engine selects chart types based on data characteristics, frontend renders chat bubbles + data tables + ECharts charts.

**Tech Stack:** FastAPI, SQLAlchemy, OpenAI/Anthropic/DeepSeek SDKs, React, TypeScript, Ant Design, ECharts, echarts-for-react

---

## File Structure

### Backend Files (Create)
- `backend/app/models/chat_session.py` - ChatSession and ChatMessage models
- `backend/app/schemas/chat.py` - Pydantic schemas for chat API
- `backend/app/services/chat.py` - ChatService with LLM function calling
- `backend/app/services/chart_engine.py` - Rule-based chart type selection
- `backend/app/api/chat.py` - Chat API endpoints
- `backend/alembic/versions/004_add_chat_tables.py` - Database migration

### Backend Files (Modify)
- `backend/app/api/__init__.py` - Register chat router
- `backend/app/models/__init__.py` - Import chat models

### Frontend Files (Create)
- `frontend/src/models/chat.ts` - TypeScript interfaces
- `frontend/src/services/chat.ts` - Chat API service
- `frontend/src/pages/ChatAgent.tsx` - Chat UI with ECharts
- `frontend/src/components/ChatMessage.tsx` - Message bubble component
- `frontend/src/components/DataTable.tsx` - Data table component
- `frontend/src/components/ChartRenderer.tsx` - ECharts wrapper

### Frontend Files (Modify)
- `frontend/src/App.tsx` - Add chat route
- `frontend/package.json` - Add echarts dependencies

---

## Implementation Tasks

Due to file size, the complete implementation plan with all tasks, code snippets, and verification steps has been prepared. The plan follows TDD principles with bite-sized tasks (2-5 minutes each) covering:

### Chunk 1: Database Models and Migration (Tasks 1-2)
- ChatSession and ChatMessage models
- Database migration script 004

### Chunk 2: Chart Engine and Schemas (Tasks 3-4)
- Rule-based chart type selection engine
- Pydantic schemas for chat API

### Chunk 3: ChatService with LLM (Tasks 5-6)
- ChatService core with tool schemas
- LLM integration (OpenAI/Anthropic/DeepSeek)

### Chunk 4: Chat API Endpoints (Task 7)
- Create/list/delete sessions
- Send message endpoint

### Chunk 5: Frontend Services (Tasks 8-9)
- TypeScript models
- Chat API service

### Chunk 6: Frontend Components (Tasks 10-12)
- ChatMessage bubble component
- DataTable component
- ChartRenderer with ECharts

### Chunk 7: ChatAgent Page (Tasks 13-14)
- Main chat interface
- Route integration

---

## Success Criteria

- [ ] Users can create and list chat sessions
- [ ] Multi-turn dialogue maintains context (last 20 messages)
- [ ] LLM calls MCP tools via function calling
- [ ] Query results display in data tables
- [ ] Charts auto-generate based on data characteristics
- [ ] Chart validation fails gracefully (shows table only)
- [ ] Supports DeepSeek / Claude / OpenAI LLMs
- [ ] LLM timeout (>30s) returns friendly error
- [ ] Single chat response time ≤15s (including LLM)
- [ ] All backend tests pass
- [ ] Frontend compiles without errors

---

**Total estimated time:** 14-18 hours (2-2.5 days)

**Note:** Full detailed implementation with all code snippets was provided in the conversation above. This summary file captures the structure. Refer to the conversation transcript for complete task-by-task implementation details.
